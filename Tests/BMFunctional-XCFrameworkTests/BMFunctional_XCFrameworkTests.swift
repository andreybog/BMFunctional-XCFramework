    import XCTest
    @testable import BMFunctional_XCFramework

    final class BMFunctional_XCFrameworkTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(BMFunctional_XCFramework().text, "Hello, World!")
        }
    }
